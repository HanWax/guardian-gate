# Environment variables for compound automation
# Copy this to .env.local and fill in values

# Option 1: Claude Code CLI (RECOMMENDED - uses your Pro subscription, no API credits)
# Just set this to true if you have claude CLI installed and authenticated
USE_CLAUDE_CODE=true

# Option 2: Anthropic API key (requires API credits - costs money)
# Only needed if USE_CLAUDE_CODE is not set
# ANTHROPIC_API_KEY=your-api-key-here

# GitHub token (optional - gh cli usually handles auth)
# GITHUB_TOKEN=your-github-token

# Slack webhook for notifications (optional)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz
